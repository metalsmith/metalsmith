#!/usr/bin/env node

var realpathSync = require("fs").realpathSync;

var path = require("path");

var join = path.join;

var dirname = path.dirname;

require(join(dirname(realpathSync(__filename)), "..", "lib", "cli"));